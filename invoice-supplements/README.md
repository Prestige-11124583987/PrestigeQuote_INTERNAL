# Invoice Supplements

Place PDF files in this folder for local use. In the hosted Render app, use the **Invoice Supplements** upload panel; files are stored on the persistent disk at `/var/data/invoice-supplements`.

All PDFs are appended automatically after the generated invoice in filename order. Prefix filenames with `01`, `02`, `03`, etc. to control the sequence.
